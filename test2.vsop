class Main {
    main() : int32 {
        let n : int32 <- 0 in
        let max : int32 <- 10 in
        while n < max do {
            printInt32(n).print("\n");
            n <- n + 1
        };
        0
    }
}